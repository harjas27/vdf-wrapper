void compute(const char *s, size_t len, char *r, size_t len_r, unsigned long difficulty, unsigned short bits);

int verify(const char *s, size_t len, const char *r, size_t len_r, unsigned long difficulty, unsigned short bits);
